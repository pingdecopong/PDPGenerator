{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Watertank</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.Name }}</td>
            </tr>
            <tr>
                <th>Systemuserid</th>
                <td>{{ entity.SystemUserId }}</td>
            </tr>
            <tr>
                <th>Integerdata</th>
                <td>{{ entity.IntegerData }}</td>
            </tr>
            <tr>
                <th>Deftest1</th>
                <td>{{ entity.DefTest1 }}</td>
            </tr>
        </tbody>
    </table>

    <form action="{{ path('watertank_delete', { 'id': entity.id }) }}" novalidate method="post" {{ form_enctype(form) }} name="watertank_delete_form">
        {{ form_widget(form) }}
        <input type="submit" value="delete"/>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('watertank') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('watertank_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <a href="{{ path('watertank_delete', { 'id': entity.id }) }}">
            Delete
        </a>
    </li>
</ul>
{% endblock %}
